{
  "name": "demo-lecturer-planner",
  "version": "1.0.0",
  "description": "A Fastify API for sending WhatsApp and SMS messages via Twilio",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc && node dist/server.js",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "fastify": "^5.4.0",
    "twilio": "^5.7.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3"
  },
  "optionalDependencies": {
    "fsevents": "*"
  }
}
